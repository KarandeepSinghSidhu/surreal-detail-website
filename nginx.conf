server {
    listen 80;
    server_name surrealdetail.com www.surrealdetail.com;

    # Redirect HTTP to HTTPS (uncomment after Certbot setup)
    # return 301 https://$host$request_uri;

    location / {
        proxy_pass http://localhost:3000;
        proxy_http_version 1.1;
        proxy_set_header Upgrade $http_upgrade;
        proxy_set_header Connection 'upgrade';
        proxy_set_header Host $host;
        proxy_set_header X-Real-IP $remote_addr;
        proxy_set_header X-Forwarded-For $proxy_add_x_forwarded_for;
        proxy_cache_bypass $http_upgrade;
    }
}

# Uncomment after running Certbot:
# server {
#     listen 443 ssl;
#     server_name surrealdetail.com www.surrealdetail.com;
#
#     ssl_certificate /etc/letsencrypt/live/surrealdetail.com/fullchain.pem;
#     ssl_certificate_key /etc/letsencrypt/live/surrealdetail.com/privkey.pem;
#
#     location / {
#         proxy_pass http://localhost:3000;
#         proxy_http_version 1.1;
#         proxy_set_header Upgrade $http_upgrade;
#         proxy_set_header Connection 'upgrade';
#         proxy_set_header Host $host;
#         proxy_cache_bypass $http_upgrade;
#     }
# }
